实体类
定义属性
--提供get、set属性访问器（如不提供get、set属性访问器，则无法对实体序列化、反序列化）
--提供空构造（如不提供空构造，则无法对实体反序列化）
提供带参构造，参数为Builder类，在构造内为所有属性赋值

定义类内部静态类Builder
定义属性
提供空构造（如有必选属性，提供带参构造，将必选属性作为构造参数，并在构造内赋值）
为所有属性提供赋值方法（返回类型为Builder，返回值为this）
静态内部类提供的setter操作,返回值是当前Builder本身